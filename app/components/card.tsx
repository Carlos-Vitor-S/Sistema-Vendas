import { StyleSheet, Text, View } from "react-native";
import React from "react";

const card = () => {
  return (
    <View>
      <Text>card</Text>
    </View>
  );
};

export default card;

const styles = StyleSheet.create({});
